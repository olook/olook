variant_number = '<%= j params[:variant_number] %>';
if(initProduct.isEmpty(variant_number)) {
  $('#msg-<%= j params[:product_id] %>').show().html('Qual é o seu tamanho mesmo?');
}
else
{
  $('#msg-<%= j params[:product_id] %>').hide();

}
