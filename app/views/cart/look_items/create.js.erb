size = '<%= j params[:size] %>';
productId =  '<%= j params[:product_id] %>';
if(initProduct.validateSize(size)) {
  $('msg-<%= j params[:product_id] %>').text("teste2");
}
else
{
  $('msg-<%= j params[:product_id] %>').text("teste");
}
