var variantNumber = '<%= j params[:variant_number] %>';
var product_price = '<%= j params[:product_price] %>';
var product_id = '<%= j params[:product_id] %>';

var variantNumbers = <%= @variant_numbers.to_s.html_safe %>;

olookApp.mediator.publish("updateMinicartInput",variantNumbers, variantNumber, '<%= escape_javascript(hidden_field_tag("variant_numbers[]", params[:variant_number])) %>');

olookApp.mediator.publish("updateMinicartDisplay", product_id, product_price, variantNumber);    
   
