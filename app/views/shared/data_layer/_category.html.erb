<script type="text/javascript">
  dataLayer.push({
    'Category': '<%= section_name pixel_information %>',
    'Page': 'List',
    'CatalogProductsIds': '<%= catalog_products.map{ |item| "#{item.id}" }.first(3).join(', ') %>',
    'CriteoBasket': [<%= raw catalog_products.map { |item|
	"{ id:'#{item.id}', price:#{number_with_precision(item.price, separator: ".", precision: 2)}, quantity:#{item.inventory} }"}.join(', ') -%>]
  });
</script>
