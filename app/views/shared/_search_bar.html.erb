<% ab_test("searchbar_text", "default", "bverde") do |style| %>
  <div class="search_box" >
   <%= form_tag(search_path, method: :get, id: "search_form") do  %>
     <%= text_field_tag "q",nil, id: "search_product", value: "O que você procura?" %>
     <%= submit_tag "Buscar", class: "#{style}" %>
   <% end %>
  </div>
<% end %>