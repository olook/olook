- if search.has_next_page?
  = link_to "proximo", catalog_path(page: search.next_page)
- if search.has_previous_page?
  = link_to "anterior", catalog_path(page: search.previous_page)

- search.pages.times do |page|
  = link_to "#{ page + 1 }", catalog_path(page: page + 1)
