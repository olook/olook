<% cart.items.each do |item| %>
_gaq.push(['_addItem',
    '<%= cart.id %>',
    '<%= item.variant.product_id %>',
    '<%= item.variant.product.name %>',
    '<%= item.variant.description %>',
    '<%= item.retail_price.to_s %>',
    '<%= item.quantity.to_s %>'
 ]);
<% end %>