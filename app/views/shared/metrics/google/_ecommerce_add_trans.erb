<% order.line_items.each do |item| %>
_gaq.push(['_addTrans',
   '<%= order.number %>',
   'olook',
   '<%= order.amount_paid %>',
   '',
   '<%= order.freight_price.to_s %>',
   '<%= order.freight_city %>',
   '<%= order.freight_state %>',
   'Brasil'
 ]);
<% end %>