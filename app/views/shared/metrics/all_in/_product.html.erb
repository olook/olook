<% availability = case product.inventory
 when 0 then "Indisponvel"
 when 1  then "Resta 1"
 else
   "Disponivel"
 end %>
<script src="//i.btg360.com.br/bs.js" type="text/javascript"></script>
<script type="text/javascript">
  try{
    __bsd["id"]="8:1";
    bs.setaPagina('<%= raw product.name%>', '<%= raw product.category_humanize %>', '','<%= raw product.try(:subcategory) %>', '<%= raw product.id %>', '<%= raw product.price %>', '', '<%= raw availability %>', '', '', '', '<%= raw product.brand %>');
  }catch(e){
  }
</script>

