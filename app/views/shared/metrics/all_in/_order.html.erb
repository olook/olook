<script src="//i.btg360.com.br/bs.js" type="text/javascript"></script>
<script type="text/javascript">
    try{
        __bsd["id"]="8:1";
        bs.setaPagina('<%= raw "#{ params[:controller] } #{ params[:action] }"%>');
        bs.setEtapaCarrinho("Confirmacao da Compra","","","","","");
        bs.setaCliente("<%= raw current_user.id %>","<%= raw current_user.email %>");
        bs.finalizaCompra("<%= raw order.amount_paid %>","<%= raw order.freight_price %>","4","<%= raw order.erp_payment.type %>","<%= raw order.erp_payment.bank %>","","<%= raw order.number %>", '<%= raw order.payments.with_discount.map { |payment| "#{ payment.type }"}.join(", ") %>');

          <% order.line_items.each do |item| %>
        bs.addProdutoComprado("<%= raw order.number %>","<%= raw item.variant.product_id %>","<%= raw item.variant.name %>","<%= raw item.retail_price %>", "", "<%= raw item.quantity %>","", "<%= raw item.variant.product.category_humanize %>", "<%= raw item.variant.product.subcategory %>", "", "","<%= raw item.variant.product.brand %>",
            "", "", "", "");
          <% end %>
    }catch(e){
    }
</script>

