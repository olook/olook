<script type="text/javascript">
var basket = {
	products: [
<%= cart.items.map { |item|
	"{ identifier:'#{item.product.id}', amount:#{number_to_currency(item.variant.retail_price).delete('R$ ')}, currency:'BRL', quantity:'#{item.quantity}' }"
}.join(', ') -%>
	]
};
</script>
