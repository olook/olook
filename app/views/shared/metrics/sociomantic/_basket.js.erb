var basket = {
products: [
<%= cart.items.each do |item| %>
{ identifier: '<%= item.id %>', amount: <%= number_to_currency(item.variant.price) %>, currency: 'BRL', quantity: <%= item.quantity %> },
<% end %>
]
};
