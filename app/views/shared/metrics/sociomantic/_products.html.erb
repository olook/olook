<script type="text/javascript">
  var moments_products = {
  products: [
              <%= products.map{ |item|
                "{identifier: '#{item.id}'}"
              }.join(', ') -%>
            ]
  };
  var categories = {
    category: [
                <%= categories_id.map{ |id|
                  "{ COLEÇÃO: '#{collection_themes_show_url(id)}'}"
                }.join(',') -%>
              ]
  };
</script>

