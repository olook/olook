<div id="shoe_size_filter">
  <%= label_tag "Mostrar números:" %>

  <span><%= current_user.shoes_size if current_user && current_user.shoes_size %></span>
  <div class="sizes">
  <ol class="column">
   <% liquidation_presenter.liquidation.shoe_sizes.each do |shoe_size| %>
    <li>
      <%= check_box_tag('shoe_sizes[]', shoe_size, false , :id => "size_#{shoe_size}") %>
      <%= label_tag(shoe_size, nil, :for => shoe_size) %>
    </li>
    <% end %>
  </ol>
  </div>

</div>

