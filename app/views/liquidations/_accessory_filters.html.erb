<div class="filter">
  <h1 class="subtitle">Acessórios</h1>
  <ol class="column">
    <% liquidation_presenter.liquidation.resume[:categories][Category::ACCESSORY].each do |key, value| %>
      <li>
        <%= check_box_tag('subcategories[]', key) %>
        <label><%= value %></label>
      </li>
    <% end %>
  </ol>
</div>


