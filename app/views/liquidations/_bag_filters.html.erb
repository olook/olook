<div class="filter">
  <h1 class="subtitle">Bolsas</h1>
  <ol class="column">
    <% liquidation_presenter.liquidation.resume[:categories][Category::BAG].each do |key, value| %>
      <li>
        <%= check_box_tag('subcategories[]', key) %>
        <label><%= value %></label>
      </li>
    <% end %>
  </ol>
</div>


