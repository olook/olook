<div class="lookbook">
  <h1>Lookbooks</h1>
  <div id="mask_carousel_showroom">
    <ul>
      <% Lookbook.where("active = 1").order("created_at DESC").each do |lookbook| %>
        <li><%= link_to image_tag(lookbook.thumb_image.try(:url)), "/lookbooks/#{lookbook.name}" %></li>
      <% end %>
    </ul>
    <%= link_to "Anterior", "javascript:void(0);", :class => "carousel-prev" %>
    <%= link_to "Próximo", "javascript:void(0);", :class => "carousel-next" %>
  </div>
</div>
