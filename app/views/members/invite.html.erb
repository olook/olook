<% content_for :head do %>
  <%= stylesheet_link_tag "section/invite" %>
<% end %>

<% content_for :footer do %>
  <%= javascript_include_tag "members" %>
<% end %>

<section id="user-info">
<hgroup class='info'>
    <% if current_user.has_facebook? %>
      <% if @user.has_fraud %>
        <p class="warn"><span>Atenção:</span> Os créditos que estavam atrelados a convidadas com CPFs inválidos, foram inabilitados.</p>
      <% end %>
    <% else %>
      <div class="connect">
        <p>Conecte-se com o facebook<br /> e veja sua vitrine antes de todas</p>
        <%= link_to "Conectar com o Facebook", user_omniauth_authorize_path(:facebook), :class => "fb-connect popup", :"data-width" => 410, :"data-height" => 300 %>
      </div>
    <% end %>
    <h2 class="<%= 'no_facebook' if current_user.has_facebook? %>"><%= display_member_credit(current_user) %></h2>
  </hgroup>

  <div class="banner">
    <h1>Quer ganhar</h1>
    <h2>R$20?</h2>
    <h3>
      <p>Convide suas amigas e ganhe R$20</p>
      <p>quando elas fizerem a 1ª compra.</p>
    </h3>
    <%= link_to 'Veja o regulamento', terms_path %>
    <%= image_tag('common/model-banner.png') %>
  </div>
</section>

<nav class="invite">
  <ul>
    <li class='personal_link'><%= link_to 'Link pessoal', 'javascript:void(0);', :class => 'share-mail' %></li>
    <li class='invite_mail'><%= link_to 'Email', 'javascript:void(0);', :class => 'invite-mail' %></li>
    <li class='facebook'><%= link_to  'Facebook', 'javascript:void(0);', :class => 'post-to-wall' %></li>
    <li class='import_contacts'><%= link_to 'Lista de contatos', 'javascript:void(0);', :class => 'import-contacts' %></li>
  </ul>
</nav>

<section id="share-mail">
  <hgroup>
    <h1>Compartilhe seu link pessoal</h1>
    <h2>Envie seu link pessoal <br /> pelo FaceBook, Twitter, MSN e <br /> por onde mais você quiser.</h2>
  </hgroup>

  <form>
    <fieldset>
      <ol>
        <li>
          <input type="text" value="<%= accept_invitation_url(:invite_token => @user.invite_token) %>" readonly="readonly">
          <ul class="share">
            <li>
              <a href="https://twitter.com/share" class="twitter-share-button" data-text="<%= twitte_message(@user) %>" data-url="www.olook.com.br" data-count="none" data-counturl="www.olook.com.br">Tweet</a>
              <script type="text/javascript" src="http://platform.twitter.com/widgets.js"></script>
              <script type="text/javascript" charset="utf-8">
                window.twttr = (function (d,s,id) {
                  var t, js, fjs = d.getElementsByTagName(s)[0];
                  if (d.getElementById(id)) return; js=d.createElement(s); js.id=id;
                  js.src="//platform.twitter.com/widgets.js"; fjs.parentNode.insertBefore(js, fjs);
                  return window.twttr || (t = { _e: [], ready: function(f){ t._e.push(f) } });
                }(document, "script", "twitter-wjs"));
              </script>
              <script type="text/javascript" charset="utf-8">
                twttr.ready(function (twttr) {
                  twttr.events.bind('click', function(event){
                    <%= track_event('MemberInvite', 'TwitterPost') %>
                  });
                });
              </script>
            </li>
            <li>
              <%= link_to image_tag('common/fb-post-to-wall.png'), 'javascript:void', id: 'facebook_post_wall', onclick: track_event('MemberInvite', 'FacebookPost') %>
            </li>
          </ul>
        </li>
      </ol>
    </fieldset>
  </form>
</section>

<section id="invite-mail">
  <hgroup>
    <h1>Convide suas amigas por email</h1>
    <h2>Digite o email de suas amigas <br /> no campo ao lado separados por vírgula.</h2>
  </hgroup>

  <%= form_tag (member_invite_by_email_path) do %>
    <fieldset>
      <ol>
        <li>
          <%= text_area_tag :invite_mail_list %>
        </li>
      </ol>
    </fieldset>
    <fieldset class="buttons">
      <ol>
        <li>
          <%= submit_tag value: '', id: 'invite_email', onclick: track_event('MemberInvite', 'EmailInvite') %>
        </li>
      </ol>
    </fieldset>
  <% end %>
</section>

<section id="post-to-wall", style="background:none;">
  <hgroup>
    <h1>Convide amigas<br /> do Facebook</h1>
    <h2>Para convidar, selecione suas amigas<br />na caixa ao lado
    </h2>
  </hgroup>
  <%= link_to image_tag('common/bt-face-send-messages.png'), 'javascript:void;', :id => 'facebook_invite_friends', onclick: track_event("MemberInvite","FacebookMessage") %>
</section>

<%#= render 'invite_by_email.html.erb' %>
