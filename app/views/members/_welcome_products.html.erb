<section id="products">
  <h1 class="title">Destaques deste mês</h1>
  <div class="products_list">
    <ul>
      <% @products.each do |product| %>
        <%= render "shared/showroom_product_item", :product => product %>
      <% end %>
    </ul>
  </div>
</section>
