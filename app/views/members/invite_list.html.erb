<div id="invite_list">
  <h1><%= invitation_score(@user) %></h1>
  <h2>A cada compra de uma amiga convidada, você ganha R$10, e ela R$10 por ter se cadastrado.</h2>
  <table id="list">
    <thead>
      <tr>
        <th>Email</th>
        <th>Convidada em</th>
        <th>Registrada</th>
      </tr>
    </thead>

    <% @invites.each do |invite| %>
      <tbody>
        <tr>
          <td><%= invite.email %></td>
          <td><%= invite.created_at.to_s(:short) %></td>
          <td><%= Invite.status_for_user_invite(@user.id, invite) %></td>
        </tr>
      </tbody>
    <% end %>
  </table>
  <%= will_paginate @invites %>

</div>
