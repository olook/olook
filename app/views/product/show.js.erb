initQuickView.inQuickView = true;
$('.content').first().append("<%= escape_javascript(render('shared/modal_login')) %>");
$('#quick_view').html("<%= escape_javascript(render(:partial => 'product/quick_view',:locals => { :product => @product, :gift => @gift, :shoe_size => @shoe_size, :only_view => @only_view})) %>");
$('#quick_view').append("<%= escape_javascript(render(:partial => 'product/facebook_js', :locals => { :facebook_app_id => @facebook_app_id, :product => @product })) %>");

$('#quick_view').fadeIn(300);

$('#quick_view').css("top", $(window).scrollTop()  + 35);
var width = $(document).width();
var height = $(document).height();
$('body').prepend("<div class='overlay'></div>");
$('.overlay').width(width).height(height);

$('#quick_view ol.colors li a').attr('data-remote', true);

if (typeof initSuggestion != 'undefined') {
  initSuggestion.checkIfProductIsAlreadySelected();
}
initQuickView.productZoom();

try{
  FB.XFBML.parse();
}catch(ex){}
