<% present 'Moment', :moment => @moment do |moment_presenter| %>
  <% content_for :head do %>
    <%= stylesheet_link_tag "section/moments" %>
  <% end %>

  <% content_for :footer do %>
    <%= javascript_include_tag "moments" %>
  <% end %>

  <% content_for :full do %>
    <div id="banner" style="background-image: url('<%= @moment.header_image %>');"></div>
    <div class="content">
      <%= render "nav", :moments => @moments %>
    </div>
  <% end %>

  <div id="moments" class="content">
    <%= moment_presenter.render_search_form %>
    <div id="products_container">
      <div id="shoe_size_filter">
        <%= label_tag "Mostrar números:" %>
        <span><%= current_user.user_info.shoes_size if current_user && current_user.user_info %></span>
        <div class="sizes">
          <ul>
            <% moment_presenter.moment.catalog.shoe_sizes.each do |key, value| %>
              <li>
                <% if current_user && current_user.user_info %>
                <%= check_box_tag('shoe_sizes_filter[]', key, (key.to_s == current_user.user_info.shoes_size.to_s ? true : false), :id => "size_#{key}") %>
                <%= label_tag(key, nil, :for => "size_#{key}") %>
                <% end %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
      <%= render "shared/filters/order" %>
      <ul id="products">
        <%= render "products", :products => @catalog_products %>
      </ul>
      <div class="loading"></div>
      <%= will_paginate @catalog_products %>
    </div>
  </div>
<% end %>
