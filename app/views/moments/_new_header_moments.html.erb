<% if is_moment_page? %>
  <% if @featured_products %>
    <%= render "shared/featured_products", :products => @featured_products, :category_id => @category_id %>
  <% end %>


  <section class="filters">
  	<h1><b>Você está em <%= print_section_name %> <span>//</span></b></h1>
  	<div id="tags">
        <ul>
          <li class="btn-clean"><button type="button" class="cleanFilter">( limpar filtros )</button></li>
        </ul>
        
    </div>
  </section>

<% end %>


<div class="exhibition-mode">
  <p>EXIBIÇÃO: <span class="selected product">Produto</span><b>/</b><span class="model">Produto com modelo</span></p>
  <%= render "shared/filters/order" %>
</div>
