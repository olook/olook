<% content_for :head do %>
  <%= stylesheet_link_tag "section/landing" %>
<% end %>

<% content_for :top_my_account, render(:partial => "shared/header_default") if @landing_page.show_header? %>

<% content_for :content do %>
  <%= content_for?(:news_content) ? yield(:news_content) : yield %>
<% end %>

<div class="header_login">
	<!-- <h1>Já faz parte? <span>Entre</span></h1> -->
	<h2><%= image_tag "landing_page/olook-landing-logo.png", :title => "Olook" %></a></h2>
</div>
<section class="landing-content">
  <img src="assets/landing_page/bg-landing-A-infos.png" class="infos" />   	
  <%= link_to @landing_page.button_url, :class => "action", :title => @landing_page.button_alt do %>
    <%= image_tag @landing_page.button_image.try(:url), :alt => @landing_page.button_alt, :style => button_position(@landing_page), data_src: @landing_page.button_hover_image.try(:url) %>
  <% end %>
</section>

<% content_for :footer do %>
  <script>
    $(function(){
      $(".bt-acesse a").mouseover(function(){
        $("img.infos").fadeIn();
      }).mouseout(function(){
        $("img.infos").fadeOut();
      })
    })
  </script>
<% end %>
<%= content_for :site_footer, render(:partial => "shared/footer") if @landing_page.show_footer? %>
