<% friends.each do |friend| %>
  <li>
    <%= link_to "#" do %>
      <div class="avatar_mask">
        <%= image_tag user_avatar(friend) %>
      </div>
      <span><%= friend.name %></span>
    <% end %>
  </li>
<% end %>
