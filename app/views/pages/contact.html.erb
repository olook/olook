<% content_for :head do %>
  <%= stylesheet_link_tag "section/contact" %>
<% end %>

<div id="contact_content">
  <h1>Entre em Contato</h1>
  <p>Seu contato é muito importante para nós. <br />Quer enviar sugestões, elogios ou tirar dúvidas? Preencha os campos ao lado e nossa equipe entrará em contato o mais rápido possível.</p>
  <ul>
    <li class="phone">(11) 2626-3489</li>
    <li class="mail"><%= link_to "falecom@olook.com.br", "mailto:falecom@olook.com.br" %></li>
  </ul>
  <p class="time">De segunda a sexta das 9:00 as 18:00</p>
</div>
<%= form_for(@contact_form, :url => send_contact_path, :html => { :id => "contact" }) do |f| %>
  <fieldset class="inputs">
    <ol>
      <% if @contact_form.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@contact_form.errors.count, "error") %> prohibited this contact message from being sent:</h2>
      
          <ul>
          <% @contact_form.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>
      <li>
        <%= f.label :email %>
        <%= f.email_field :email %>
      </li>
      <li>
        <%= f.label :subject, "Assunto" %>
        <%= f.select(:subject, options_from_collection_for_select(@contact_infos, "id", "title"), {}, {:class => "custom_select"}) %>
      </li>
      <li>
        <%= f.label :message, "Mensagem" %>
        <%= f.text_area :message %>
      </li>
    </ol>
  </fieldset>
  <fieldset class="buttons">
    <ol>
      <li>
        <%= f.submit "" %>
      </li>
    </ol>
  </fieldset>
<% end %>

