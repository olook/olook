<% content_for :head do %>
  <%= stylesheet_link_tag "section/collections" %>
<% end %>

<%= render "menu", collection_theme_groups: @collection_theme_groups %>
<% content_for :footer do %>
  <%= javascript_include_tag "moments" %>
<% end %>

<section id="collections_content">
  <div class="collections_header"></div>
  <div class="collections_filters"></div>
  <div class="collections_products">
    <% @catalog_products.each do |cp| %>
      <%= render "shared/moment_product_item", :product => cp.product %>
    <% end %>
    <%= "Nenhum produto disponível" if @catalog_products.empty? %>
  </div>
</section>

