$("table#total tr.total td.value p:not('.limit')").text("<%= number_to_currency @cart_service.total %>");
<% if @cart_service.is_minimum_payment? %>
  $("table#total p.limit").addClass("show");
<% else %>
  $("table#total p.limit").removeClass("show");
<% end %>
