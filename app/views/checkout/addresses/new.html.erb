<% content_for :head do %>
  <%= stylesheet_link_tag "section/checkout" %>
<% end %>

<section id="address_new" class="checkout">
<%= form_for(@address, :url => cart_checkout_addresses_path, :html => { :class => 'address_new_edit' }) do |f| %>
    <section class="main">
      <h1>Adicione um endereço</h1>
      <%= render :partial => "shared/errors", :locals => { :object => @address } %>
      <%= render "shared/address/form", :address => @address, :f => f %>
      <%= f.submit '', :class => "save" %>
    </section>
    <section class="sidebar">
      <%= render "shared/payments/info_sidebar", :cart => @cart, :cart_service => @cart_service, :has_payment => false %>
    </section>
  <% end %>
</section>
