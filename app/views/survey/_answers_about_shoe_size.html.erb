  <h3>Qual o tamanho do seu sapato?</h3>
  <ul class="question">
    <% question.answers.each do |answer| %>
    <li>
      <label for="<%= "questions_question_#{answer.question.id}_#{answer.id}" %>"> <%= answer.title %>
        <%= radio_button_tag("questions[question_#{answer.question.id}]", "#{answer.id}") %>
        </label>
      </li>
    <% end %>
  </ul>

