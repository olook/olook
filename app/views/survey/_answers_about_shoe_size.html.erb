  <h3>Qual o tamanho do seu sapato?</h3>
  <ul class="question">
    <% question.answers.each do |answer| %>
    <li>
      <label for="<%= "questions_question_foot_#{answer.id}" %>"> <%= answer.title %>
        <%= radio_button_tag("questions[question_foot]", "#{answer.id}") %>
        </label>
      </li>
    <% end %>
  </ul>

