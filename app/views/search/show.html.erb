<% content_for :head do %>
  <%= stylesheet_link_tag "new_structure/section/search" %>
<% end %>

<div class="search">
  
  <% if @hits["found"] == 0 %>
    <h2><b>Poxa, ainda não temos <span><%= @q%></span> :( </b></br>Como sempre chegam novidades, logo mais devemos ter o que você procura por aqui. Fique ligada! ;)</h2>
    <p class="hints">Sugestões para uma próxima busca:</p>
    <ul class="hint-list">
      <li class="hint_one">Verifique se a ortografia está correta;</li>
      <li class="hint_two">Tente palavras mais genéricas;</li>
      <li class="hint_three">Ou palavras diferentes, com o mesmo significado;</li>
      <li class="hint_four">E remova aspas ou sinais de mais: isso restringe a possibilidade de resultados.</li>
    </ul>
    
  <% else %>
    <div class="cb">
      <h2 class="title-founded">Legal! Temos <span><%= @hits["found"] %></span> opções de <span><%= @q%></span> para você :)</h2>
    </div>

    <% if @show_filters -%>
      <div id="filters">
        
        <% @model_names.each do |category, models| %>
          <p class="title-category"><%= category %></p>
          <ul class="subcategory-list">
          <% models.each do |model,amount| -%>
            <li><%= link_to "#{model} (#{amount})", "show?q=#{@q}&category=#{model}" %></li>
          <% end -%>
          </ul> 
        <% end %>

        <p class="title-category">Marcas</p>
        <ul class="subcategory-list">
        <% @brands.each do |brand,amount| -%>
          <li><%= link_to "#{brand} (#{amount})", "show?q=#{@q}&brand=#{brand}" %></li>
        <% end -%>
        </ul>
      
      </div>
    <% end %>

    <div id="search_result">
      <ul class="search-results-list">  
        <% @products.each do |product| %>
          <%= render "shared/searched_product_item", product: product%>
        <% end %>
      </ul>
    </div>
    
  <% end %>
</div>

<% if @hits["found"] == 0 %>
  <div class="other-products">
    <p>Enquanto isso, porque você não dá uma olhada em...</p>
  </div>
  <div class="chaordic mostpopular" data-skin="MP-top-5"></div>
  <%= render "home/stylists" %>
<% end %>
<%= render "home/boxes" %>
<%= render "home/products_categories" %>
<%= render "home/facebook_box" %>





