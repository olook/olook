<% self.formats = ["html"] %>
{
  "startDate":"<%= timeline_date_format(event.created_at) %>",
  "endDate":"",
  "headline":"Separando | atualizado as <%= headline_time_format(event.created_at) %>",
  "text":<%= render(:partial => "admin/orders/timeline/#{event.to}/#{event.to}", :locals => {:order => order, :event =>  event}).to_json.html_safe %>,
  "asset":
  {
    "media":"",
    "credit":"",
    "caption":""
  }
}