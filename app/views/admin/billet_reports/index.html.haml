%h1 Boletos a expirar em 24 horas
%div.inner
  %table.table
    %tr
      %th Cliente
      %th Pedido
      %th Email
      %th Telefone
      %th Total pago

    - @billets.all.each do |billet|
      %tr
        %td= link_to billet.order.user.name, admin_user_path(billet.order.user) if billet.order.try(:user)
        %td= link_to billet.order.number, admin_order_path(billet.order) if billet.order
        %td= link_to billet.order.user.email, admin_user_path(billet.order.user) if billet.order.try(:user)
        %td= billet.order.user.try(:addresses).try(:first).try(:telephone) if billet.order
        %td= billet.order.amount_paid  if billet.order

  %br
