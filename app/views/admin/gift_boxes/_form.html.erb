<%= form_for([:admin,@gift_box], html: {multipart: true}) do |f| %>
  <% if @gift_box.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gift_box.errors.count, "error") %> prohibited this landing page from being saved:</h2>

      <ul>
      <% @gift_box.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
  <%= f.label :ativo %><br />
    <%= f.check_box :active %>
  </div>
  <div class="field">
    <%= f.label :thumb_imagem %><br />
    <%= f.file_field :thumb_image %><br />
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

