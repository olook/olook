<h2>Editing product</h2>

<div class="inner">
  
  <%= form_for([:admin,  @liquidation, @liquidation_product], :url => admin_liquidation_product_path) do |f| %>
    <% if @liquidation_product.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@liquidation_product.errors.count, "error") %> prohibited this liquidation product from being saved:</h2>
        <ul>
        <% @liquidation_product.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <div class="field">
      <%= f.label :discount_percent %><br />
      <%= f.text_field :discount_percent %>
    </div>
    <div class="field">
      <%= f.label :retail_price %><br />
      <%= f.text_field :retail_price %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
   <% end %>

  <br/>

   <p><%= link_to 'Back', admin_liquidation_products_path(@liquidation)%></tp>

</div>