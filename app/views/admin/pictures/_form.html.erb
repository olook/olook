<%= form_for([:admin, @picture.product, @picture]) do |f| %>
  <% if @picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@picture.errors.count, "error") %> prohibited this picture from being saved:</h2>

      <ul>
      <% @picture.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :display_on %><br />
    <%= f.select :display_on, DisplayPictureOn.to_a %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
